apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubes
  namespace: vin
  labels:
    app: kube
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kube
  template:
    metadata:
      labels: firstacs.azurecr.io/yolo
        app: kube
    spec:
      containers:
      - name: kube
        image: 
        ports:
        - containerPort: 5000
      imagePullSecrets:
      - name: vineet 